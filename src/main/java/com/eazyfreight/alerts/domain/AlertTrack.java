package com.eazyfreight.alerts.domain;

/** Which operational track the condition belongs to. Used to group the dashboard. */
public enum AlertTrack {
    LOGISTICS,
    COMPLIANCE,
    FINANCE,
    BOOKING,
    DOCUMENTATION
}
