info:
  name: Get Bookings Awaiting Truck Dispatch
  type: http
  seq: 27

http:
  method: GET
  url: "{{baseUrl}}/api/bookings/awaiting-truck-dispatch"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
