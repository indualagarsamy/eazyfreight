info:
  name: Get All Bookings
  type: http
  seq: 24

http:
  method: GET
  url: "{{baseUrl}}/api/bookings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
