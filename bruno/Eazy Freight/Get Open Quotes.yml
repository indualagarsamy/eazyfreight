info:
  name: Get Open Quotes
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/quotes/open"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
