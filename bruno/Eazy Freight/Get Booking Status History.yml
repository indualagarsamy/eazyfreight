info:
  name: Get Booking Status History
  type: http
  seq: 23

http:
  method: GET
  url: "{{baseUrl}}/api/bookings/{{bookingId}}/status-history"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
