info:
  name: Get Booking By Id
  type: http
  seq: 22

http:
  method: GET
  url: "{{baseUrl}}/api/bookings/{{bookingId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
