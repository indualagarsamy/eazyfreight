info:
  name: Get Expiring Quotes
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/quotes/expiring?withinDays=3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
