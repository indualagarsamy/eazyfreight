info:
  name: Generate Truck Delivery Order
  type: http
  seq: 17

http:
  method: POST
  url: "{{baseUrl}}/api/bookings/{{bookingId}}/truck-delivery-order"
  headers:
    - name: Content-Type
      value: application/json
    - name: X-Actor
      value: ops.jane
  body:
    type: json
    data: |-
      {
        "deliveryAddress": "Port of Nhava Sheva, Terminal 2"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
