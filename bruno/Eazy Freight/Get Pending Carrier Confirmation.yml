info:
  name: Get Pending Carrier Confirmation
  type: http
  seq: 25

http:
  method: GET
  url: "{{baseUrl}}/api/bookings/pending-carrier-confirmation"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
