info:
  name: Send Quote
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/quotes/{{quoteId}}/send"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
